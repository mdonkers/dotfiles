[Service]
# Avoid btintel_pcie's unreliable D3 transition while writing/restoring the
# hibernation image. ExecStopPost also runs when hibernation is rolled back.
ExecStartPre=/usr/local/bin/hibernate-device-hook pre
ExecStopPost=/usr/local/bin/hibernate-device-hook post
