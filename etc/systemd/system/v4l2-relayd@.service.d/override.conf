[Service]
# libcamera software ISP needs a dma-buf allocator; the packaged DevicePolicy=closed
# allow-list omits the dma-heap + udmabuf providers, so debayering was disabled -> black.
DeviceAllow=/dev/dma_heap/system rw
DeviceAllow=/dev/udmabuf rw
DeviceAllow=/dev/dri/renderD128 rw
