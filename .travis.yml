---
  language: bash
  sudo: false
  notifications:
    email: false
  addons:
    apt:
      sources:
      - debian-sid    # Grab ShellCheck from the Debian repo
      packages:
      - shellcheck
  script:
    - ./test.sh
